(()=>{const shuffle=a=>{for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a};function button(label,fn){const b=document.createElement('button');b.className='btn';b.textContent=label;b.onclick=fn;return b}function renumber(box){[...box.querySelectorAll('.quiz')].forEach((q,i)=>{const h=q.querySelector('h3');if(h)h.textContent=h.textContent.replace(/^\d+\.\s*/,`${i+1}. `)})}function addPlatform(){const box=document.getElementById('quizBox');if(!box)return;const card=box.closest('.card');if(!card||card.querySelector('.random-platform'))return;const row=document.createElement('div');row.className='filters random-platform';const b=button('🔀 隨機排序',()=>{const items=[...box.querySelectorAll('.quiz')];shuffle(items).forEach(x=>box.appendChild(x));renumber(box)});row.appendChild(b);box.before(row)}function addHandout(){const candidates=[...document.querySelectorAll('.card')];candidates.forEach(card=>{const qs=[...card.querySelectorAll('.q,.quiz')];if(qs.length<2||card.querySelector('.random-handout'))return;const row=document.createElement('div');row.className='random-handout';row.style='text-align:center;margin:12px 0';row.appendChild(button('🔀 本區隨機排序',()=>{const parent=qs[0].parentElement,live=[...parent.children].filter(x=>x.matches&&x.matches('.q,.quiz'));shuffle(live).forEach(x=>parent.appendChild(x));renumber(parent)}));const h=card.querySelector('h2');if(h)h.after(row)})}function boot(){addPlatform();addHandout();setTimeout(addPlatform,300);setTimeout(addHandout,300)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot()})();